@{
    string current = ViewContext.RouteData.Values["Action"].ToString().ToLower();
    bool hideLoginBtn = current == "login" || current == "register" || current == "forgotpassword";
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    <style>
        :root {
            --royal-1: #071028;
            --royal-2: #0b1c3f;
            --gold-1: #cfa23a;
            --gold-2: #f5d26a;
            --text: #ffffff;
        }

        .navbar-custom {
            padding: 17px;
            background: linear-gradient(135deg, var(--royal-1), var(--royal-2));
            border-bottom: 1px solid rgba(207,162,58,0.3);
        }

        .navbar-brand {
            color: var(--gold-2) !important;
            font-weight: 800;
            font-size: 1.8rem;
        }

        .navbar-custom .navbar-nav > li > a {
            color: var(--gold-1);
            font-weight: 600;
        }

        .login-btn {
            background: var(--gold-1);
            color: #071028;
            padding: 10px 18px;
            border-radius: 10px;
            font-weight: 700;
        }

        .modal-content {
            background: #0c1733;
            padding: 30px;
            border-radius: 16px;
            border: 1px solid rgba(245,210,106,0.4);
            color: white;
        }

        footer { display: none !important; }
    </style>
</head>

<body>

    <!-- NAVBAR -->
    <div class="navbar navbar-inverse navbar-fixed-top navbar-custom">
        <div class="container">

            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>

                @Html.ActionLink("Stay-Ease", "Index", "Home", null, new { @class = "navbar-brand" })
            </div>

            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("About", "About", "Home")</li>
                    <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                </ul>

                <div class="navbar-right">

                    @if (Session["FullName"] != null)
                    {
                        <span style="color:#f5d26a; margin-right:10px;">Welcome, @Session["FullName"]!</span>
                        <button class="login-btn" onclick="window.location.href='@Url.Action("Logout","Account")'">Logout</button>
                    }
                    else if (!hideLoginBtn)
                    {
                        <button class="login-btn" data-toggle="modal" data-target="#loginModal">Login</button>
                    }

                </div>
            </div>
        </div>
    </div>

    <!-- MAIN CONTENT -->
    <div class="container body-content" style="margin-top:100px;">
        @RenderBody()
    </div>

    <!-- LOGIN POPUP -->
    <div class="modal fade" id="loginModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">

                <h2 style="text-align:center; color:#f5d26a; font-weight:700;">Login</h2>

                @using (Html.BeginForm("Login", "Account", FormMethod.Post))
                {
                    <label style="color:#f5d26a;">Email / Username</label>
                    @Html.TextBox("email", null, new { @class = "form-control", placeholder = "Enter your email" })

                    <br />

                    <label style="color:#f5d26a;">Password</label>
                    @Html.Password("password", null, new { @class = "form-control", placeholder = "Enter your password" })

                    <br />

                    <button class="login-btn" style="width:100%;">Login</button>

                    <div style="text-align:center; margin-top:10px;">
                        <a href="/Account/ForgotPassword" style="color:#f5d26a;">Forgot Password?</a><br />
                        <a href="/Account/Register" style="color:#f5d26a;">New User? Register Here</a>
                    </div>
                }

            </div>
        </div>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")

</body>
</html>
